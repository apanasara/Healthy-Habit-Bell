package com.habitbell.app.data.model

enum class ThemeMode {
    AMOLED,       // Pure #000000, zero-pixel power for OLED
    EYE_COMFORT,  // Warm tones, soft amber text, blue-light reduction
    DARK,         // Standard dark
    LIGHT         // Standard daytime
}
